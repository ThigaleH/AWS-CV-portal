import React from 'react';

const Header = () => (
  <header style={{ textAlign: 'center', padding: '1rem' }}>
    <h1>CV Upload Portal</h1>
    <p>Upload, Parse & Manage Candidate Resumes</p>
  </header>
);

export default Header;
